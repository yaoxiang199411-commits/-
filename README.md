# 活动开启查询客户端

这是从 `lianglianglin.github.io/activity-schedule.html` 独立拆出的本地版本。

特点：

- 不依赖原站点首页、导航、页脚
- 目录独立，后续可以单独初始化 Git 仓库
- 保留原有本地导入 `H_活动.xml` 的查询逻辑
- 直接打开 `index.html` 即可使用

当前目录已经可以作为独立 Git 仓库维护。

建议后续流程：

- 在这个目录下单独提交改动
- 再关联你自己的远程仓库
- 后续页面迭代只改这里，不再动原站点页面
